# Ubuntu 22.04 / Python 3.10 dependencies for AI chat and local JetArm MCP.
# Keep this file independent from Windows camera and visualization packages.
openai>=1.68,<3
# Support ALL_PROXY/HTTPS_PROXY values such as socks://127.0.0.1:7897.
httpx[socks]>=0.27,<1
python-dotenv>=1.0,<2
numpy>=1.23.5,<2; python_version < "3.11"
pyserial>=3.5,<4
# RGB-only Orbbec color-frame conversion and JPEG encoding for MCP image results.
opencv-python-headless>=4.8,<4.12
# Stable MCP Python SDK line; v2 is still pre-release.
mcp>=1.27,<2
