# Windows development dependencies for the JetArm Gemini project.
# Rebuild the local .venv-gemini environment with:
#   pip install -r project\requirements.txt
numpy==2.4.6
opencv-python==4.13.0.92
pyserial>=3.5
pygame==2.6.1

# AI terminal dependencies are shared with Ubuntu through this smaller file.
-r requirements-ai.txt

# Used for the pyorbbecsdk2 diagnostic path. The current Gemini Pro Plus
# Windows route mainly uses bundled OpenNI samples plus UVC/OpenCV.
pyorbbecsdk2==2.1.1
